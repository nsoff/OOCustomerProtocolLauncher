# OOCustomerProtocolLauncher
是一个使用 .NET 10 开发的轻量级开源工具。它旨在解决一个常见的用户需求：如何让一个简单的网页链接或外部指令直接触发用户本地安装的 Microsoft Office（如 Excel、Word、PowerPoint）来打开指定的网络文件或本地文件。  传统的 Web 链接（http:// 或 https://）点击后通常会在浏览器中下载或尝试在线预览文件，而无法直接调用本地应用。本程序通过注册并监听自定义 URL 协议（例如 myoffice://），巧妙地绕过了这一限制，实现了无缝的本地应用启动体验
